#!/usr/bin/env bash
# Scratch template: intentionally empty.
#
# The base layer already provides gh, Docker, Node, nub, uv, herdr and Claude
# Code, which is everything an ad-hoc session needs.
set -euo pipefail
echo "[scratch] nothing to do — base toolchain is sufficient"
